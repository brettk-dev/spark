import { shallowMount } from "@vue/test-utils";
import {{name}} from "./{{name}}.vue";

describe("{{name}}", () => {
  let component;
  beforeEach(() => {
    component = shallowMount({{name}});
  });
});
